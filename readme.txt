npm install -- instala las dependencias de npm necesarias para inciar el proyecto
node app.js -- inicia el proyecto con node, se puede ver en localhost:3000
nodemon (opcional) para no tener que reiniciar el proyecto siempre que cambias el backend
